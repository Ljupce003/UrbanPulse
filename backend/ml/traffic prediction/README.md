# Instructions

Simply run the ```train.py``` file and wait for the model to train on the ```merged_dataset.csv.```. After the training is complete, in the model folder there should be the ```lightgbm_model.pkl```.